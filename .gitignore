node_modules/
.env
frontend/.env
# --- Add these lines to your existing .gitignore ---
# (if you don't have a .gitignore yet, this file's contents can BE your .gitignore)

# Build output — regenerated by `npm run build`, should never be committed
frontend/dist/
dist/

# Generated at build time by scripts/generate-routes.js — regenerated every build
frontend/scripts/routes.json

# Dependencies
node_modules/

# Environment files — may contain secrets (API keys, DB URLs, etc.)
.env
.env.local
.env.production
migration/.env

# Migration run logs — these look like they may contain real production data
migration/migration-dry-run.json
migration/migration-live-run-*.json
product-*.html
